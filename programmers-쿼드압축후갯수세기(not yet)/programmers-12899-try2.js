const solution = (arr) => {

    const divisor = (space) => {
        const mid = space.length / 2 - 1;

    }
    const compressor = (arr) => {

    }

    const spaceChecker = (space) => {
        // compare every element of input space whether they are all same or nots
        const flag = space[0][0];
        return space.every(innerSpace => innerSpace.every(elemet => element === flag));
    }
}