// test case 중에 'hello      world' 같은 애들도 있어서 그냥 반복문으로 풀기로 했다
const solution = (s) => {
    for (let i = 0; i < s.length - 1; i++) {
        //1. case of this index is word's start...
        if ()
    }
}